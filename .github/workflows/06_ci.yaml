## I am working on it not yet but it will be done soon.
name: 🌟 CI
run-name: 🌟CI with bundle parameter

on: 
  push:

jobs:
  initialize:
    runs-on: ubuntu-latest
    outputs:
      bundle_id: ${{ steps.get_bundleid.outputs.bundle_id }}
    steps:
      - name: get bundleid
        id: get_bundleid
        run: |
          echo '{"bundle_id": "xxx"}' > props.json
          bundle_id=$(jq -r '.bundle_id' props.json)
          echo "::set-output name=bundle_id::$bundle_id"
 
  build-rocky:
    needs: initialize
    runs-on: ubuntu-latest
    timeout-minutes: 10
    env:
      BUNDLE_ID: ${{needs.initialize.outputs.bundle_id}}
    steps:
    - name: build-rocky with initialize
      run:  |
        echo "initialize bundle_id=$BUNDLE_ID"
  
  build-debian:
    needs: initialize
    runs-on: ubuntu-latest
    timeout-minutes: 10
    env:
      BUNDLE_ID: ${{needs.initialize.outputs.bundle_id}}
      PLATFORM: debian
    steps:
      - uses: actions/checkout@v4
      - run: echo "build-debian >>bundle_id=$BUNDLE_ID, platform=$PLATFORM" 

  build-wgo-core:
    needs: initialize
    runs-on: ubuntu-latest
    timeout-minutes: 10
    env:
      BUNDLE_ID: ${{needs.initialize.outputs.bundle_id}}
      PLATFORM: debian
    steps:
      - uses: actions/checkout@v4
      - run: echo "build-wgo-core >> bundle_id=$BUNDLE_ID"

  docker-wgo-core:
    needs: [initialize, build-rocky, build-wgo-core, build-debian]
    runs-on: ubuntu-latest
    timeout-minutes: 10
    env:
      BUNDLE_ID: ${{needs.initialize.outputs.bundle_id}}
      PLATFORM: debian
    steps:
      - uses: actions/checkout@v4
      - run: echo "docker-wgo-core >>bundle_id=$BUNDLE_ID, platform=$PLATFORM"
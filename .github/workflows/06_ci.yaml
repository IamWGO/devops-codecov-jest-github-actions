## How to create and pass initialize value in workflow
name: 🌟 CI
run-name: 🌟CI with bundle parameter

on: 
  push:

jobs:
  initialize:
    runs-on: ubuntu-latest
    outputs:
      bundle_id: ${{ steps.get_bundleid.outputs.bundle_id }}
    steps:
      - name: get bundleid
        id: get_bundleid
        run: |
          echo '{"bundle_id": "xxx"}' > props.json
          bundle_id=$(jq -r '.bundle_id' props.json)
          echo "bundle_id=$bundle_id" >> $GITHUB_OUTPUT
 
  build-rocky:
    needs: initialize
    runs-on: ubuntu-latest
    timeout-minutes: 10
    env:
      BUNDLE_ID: ${{needs.initialize.outputs.bundle_id}}
    steps:
    - name: build-rocky with initialize
      run:  |
        echo "initialize bundle_id=$BUNDLE_ID"
  
  build-debian:
    needs: initialize
    runs-on: ubuntu-latest
    timeout-minutes: 10
    env:
      BUNDLE_ID: ${{needs.initialize.outputs.bundle_id}}
    steps:
      - uses: actions/checkout@v4
      - run: echo "build-debian >>bundle_id=$BUNDLE_ID" 

  build-wgo-core:
    needs: initialize
    runs-on: ubuntu-latest
    timeout-minutes: 10
    env:
      BUNDLE_ID: ${{needs.initialize.outputs.bundle_id}}
    steps:
      - uses: actions/checkout@v4
      - run: echo "docker-wgo-core >> bundle_id=$BUNDLE_ID"

  docker-wgo-core:
    needs: [initialize, build-rocky, build-debian, build-wgo-core]
    runs-on: ubuntu-latest
    timeout-minutes: 10
    env:
      BUNDLE_ID: ${{needs.initialize.outputs.bundle_id}}
    steps:
      - uses: actions/checkout@v4
      - run: echo "docker-wgo-core >>bundle_id=$BUNDLE_ID, platform=$PLATFORM"